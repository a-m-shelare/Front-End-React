import logo from "./logo.svg";
import "./App.css";

function App() {
  return (
    <div>
      <article>
        second way to create react application first check if node is install or
        not for that use command promnt or anyouther terminal like windows
        powershell node -v navicate into directory cd documents cd tuts npm
        create-react-app akash-blogs then enter cd akash-blogs code .
        <p>
          <h1>akash shelare </h1>
        </p>
      </article>
    </div>
  );
}

export default App;
